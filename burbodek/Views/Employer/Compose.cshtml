@{
    ViewData["Title"] = "Messages";
    Layout = "~/Views/Shared/_LayoutEmployer.cshtml";
}
<div class="d-flex flex-column flex-lg-row">
    <!--begin::Sidebar-->
    <div class="d-none d-lg-flex flex-column flex-lg-row-auto w-100 w-lg-275px" data-kt-drawer="true" data-kt-drawer-name="inbox-aside" data-kt-drawer-activate="{default: true, lg: false}" data-kt-drawer-overlay="true" data-kt-drawer-width="225px" data-kt-drawer-direction="start" data-kt-drawer-toggle="#kt_inbox_aside_toggle">

        <!--begin::Sticky aside-->
        <div class="card card-flush mb-0" data-kt-sticky="true" data-kt-sticky-name="inbox-aside-sticky" data-kt-sticky-offset="{default: false, xl: '100px'}" data-kt-sticky-width="{lg: '275px'}" data-kt-sticky-left="auto" data-kt-sticky-top="100px" data-kt-sticky-animation="false" data-kt-sticky-zindex="95">
            <!--begin::Aside content-->
            <div class="card-body">
                <!--begin::Button-->
                <a asp-controller="Employer" asp-action="Compose" class="btn btn-primary fw-bold w-100 mb-8">New Message</a>
                <!--end::Button-->
                <div class="menu menu-column menu-rounded menu-state-bg menu-state-title-primary menu-state-icon-primary menu-state-bullet-primary mb-10">
                    <div class="menu-item mb-3"><a asp-action="Message" class="menu-link active"><span class="menu-icon"><i class="ki-duotone ki-sms fs-2 me-3"><span class="path1"></span><span class="path2"></span></i></span><span class="menu-title fw-bold">Inbox</span><span class="badge badge-light-success">@ViewBag.InboxCount</span></a></div>
                    <div class="menu-item mb-3"><a asp-action="MarkedEmail" class="menu-link"><span class="menu-icon"><i class="ki-duotone ki-abstract-23 fs-2 me-3"><span class="path1"></span><span class="path2"></span></i></span><span class="menu-title fw-bold">Marked</span></a></div>
                    <div class="menu-item mb-3"><a asp-action="DraftEmail" span class="menu-link"><span class="menu-icon"><i class="ki-duotone ki-file fs-2 me-3"><span class="path1"></span><span class="path2"></span></i></span><span class="menu-title fw-bold">Draft</span><span class="badge badge-light-warning">@ViewBag.DraftCount</span></a></div>
                    <div class="menu-item mb-3"><a asp-action="EmailSent" class="menu-link"><span class="menu-icon"><i class="ki-duotone ki-send fs-2 me-3"><span class="path1"></span><span class="path2"></span></i></span><span class="menu-title fw-bold">Sent</span></a></div>
                    <div class="menu-item"><a asp-action="TrashedEmail" span class="menu-link"><span class="menu-icon"><i class="ki-duotone ki-trash fs-2 me-3"><span class="path1"></span><span class="path2"></span><span class="path3"></span><span class="path4"></span><span class="path5"></span></i></span><span class="menu-title fw-bold">Trash</span></a></div>
                </div>
            </div>
            <!--end::Aside content-->
        </div>
        <!--end::Sticky aside-->
    </div>
    <!--end::Sidebar-->
    <!--begin::Content-->
    <div class="flex-lg-row-fluid ms-lg-7 ms-xl-10">

        <!--begin::Card-->
        <div class="card">
            <div class="card-header d-flex align-items-center justify-content-between py-3">
                <h2 class="card-title m-0">
                    Compose Message
                </h2>

                <!--begin::Toggle-->
                <a href="#" class="btn btn-sm btn-icon btn-color-primary btn-light btn-active-light-primary d-lg-none" data-bs-toggle="tooltip" data-bs-dismiss="click" data-bs-placement="top" id="kt_inbox_aside_toggle" aria-label="Toggle inbox menu" data-bs-original-title="Toggle inbox menu" data-kt-initialized="1">
                    <i class="ki-duotone ki-burger-menu-2 fs-3 m-0"><span class="path1"></span><span class="path2"></span><span class="path3"></span><span class="path4"></span><span class="path5"></span><span class="path6"></span><span class="path7"></span><span class="path8"></span><span class="path9"></span><span class="path10"></span></i>
                </a>
                <!--end::Toggle-->
            </div>

            <div class="card-body p-0">
                <form id="kt_inbox_compose_form" asp-action="SendEmail" method="post" enctype="multipart/form-data">
                    <!--begin::Body-->
                    <div class="d-block">
                        <!--begin::To-->
                        <div class="d-flex align-items-center border-bottom px-8 min-h-50px">
                            <div class="text-gray-900 fw-bold w-75px">To:</div>
                            <input type="text" class="form-control border-0" name="ToRecipients" id="compose_to" data-kt-inbox-form="tagify">
                            <div class="ms-auto w-75px text-end">
                                <span class="text-muted fs-bold cursor-pointer text-hover-primary me-2" data-kt-inbox-form="cc_button">Cc</span>
                                <span class="text-muted fs-bold cursor-pointer text-hover-primary" data-kt-inbox-form="bcc_button">Bcc</span>
                            </div>
                        </div>
                        <!--end::To-->
                        <!--begin::CC-->
                        <div class="d-none align-items-center border-bottom ps-8 pe-5 min-h-50px" data-kt-inbox-form="cc">
                            <div class="text-gray-900 fw-bold w-75px">Cc:</div>
                            <input type="text" class="form-control border-0" name="CcRecipients" id="compose_cc" data-kt-inbox-form="tagify">
                            <span class="btn btn-clean btn-xs btn-icon" data-kt-inbox-form="cc_close">
                                <i class="ki-duotone ki-cross fs-5"></i>
                            </span>
                        </div>
                        <!--end::CC-->
                        <!--begin::BCC-->
                        <div class="d-none align-items-center border-bottom ps-8 pe-5 min-h-50px" data-kt-inbox-form="bcc">
                            <div class="text-gray-900 fw-bold w-75px">Bcc:</div>
                            <input type="text" class="form-control border-0" name="BccRecipients" id="compose_bcc" data-kt-inbox-form="tagify">
                            <span class="btn btn-clean btn-xs btn-icon" data-kt-inbox-form="bcc_close">
                                <i class="ki-duotone ki-cross fs-5"></i>
                            </span>
                        </div>
                        <!--end::BCC-->
                        <!--begin::Subject-->
                        <div class="border-bottom">
                            <input class="form-control form-control-transparent border-0 px-8 min-h-45px" name="Subject" placeholder="Subject">
                        </div>
                        <!--end::Subject-->
                        <!--begin::Message-->
                        <div id="kt_inbox_form_editor" class="bg-transparent border-0 h-350px px-3 ql-container ql-snow">
                            <div class="ql-editor ql-blank" contenteditable="true" data-placeholder="Type your message here..."></div>
                        </div>
                        <textarea hidden name="Body" id="Body"></textarea>
                        <!--end::Message-->
                        <!--begin::Attachments-->
                        <div class="dropzone dropzone-queue px-8 pt-4 pb-0" id="kt_inbox_reply_attachments" data-kt-inbox-form="dropzone">
                            <div class="dropzone-items pb-0"></div>
                        </div>
                        <!--end::Attachments-->
                    </div>
                    <!--end::Body-->
                    <!--begin::Footer-->
                    <div class="d-flex flex-stack flex-wrap gap-2 py-5 ps-8 pe-5 border-top">
                        <div class="d-flex align-items-center me-3">
                            <div class="btn-group me-4">
                                <button type="submit" class="btn btn-primary fs-bold px-6" id="btnSend">
                                    <span class="indicator-label">Send</span>
                                    <span class="indicator-progress">
                                        Please wait...
                                        <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                                    </span>
                                </button>
                                <button type="button" class="btn btn-light fs-bold px-6" id="btnSaveDraft">Save Draft</button>
                            </div>
                            <span class="btn btn-icon btn-sm btn-clean btn-active-light-primary me-2 dz-clickable" id="btnAttachment">
                                <i class="ki-duotone ki-paper-clip fs-2 m-0"></i>
                            </span>
                        </div>

                        <div class="d-flex align-items-center">
                            <a asp-action="Index" class="btn btn-icon btn-sm btn-clean btn-active-light-primary" title="Discard">
                                <i class="ki-duotone ki-trash fs-2"></i>
                            </a>
                        </div>
                    </div>
                    <!--end::Footer-->
                </form>
            </div>
        </div>
        <!--end::Card-->

    </div>
    <!--end::Content-->
</div>
@section Scripts {
    <!-- Tagify -->
    <script src="https://cdn.jsdelivr.net/npm/@@yaireo/tagify"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@@yaireo/tagify/dist/tagify.css">

    <!-- Quill -->
    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">

    <!-- Dropzone (optional for attachments) -->
    <script src="https://cdn.jsdelivr.net/npm/dropzone@5/dist/min/dropzone.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/dropzone@5/dist/min/dropzone.min.css" rel="stylesheet" />

    <script>
        $(function () {
        // Initialize Quill
        var quill = new Quill('#kt_inbox_form_editor', { theme: 'snow' });

        // Sync Quill to hidden textarea before submit
        $('#kt_inbox_compose_form').on('submit', function (e) {
            e.preventDefault();

            // Get Quill content
            $('#Body').val(quill.root.innerHTML);

            // Parse Tagify data to extract emails only
            if (toTagify && toTagify.value.length > 0) {
                const toEmails = toTagify.value.map(tag => {
                    // Extract email from "Name <email>" format or just email
                    const match = tag.value.match(/<(.+?)>/);
                    return match ? match[1] : tag.email || tag.value;
                });
                $('#compose_to').val(toEmails.join(','));
            } else {
                $('#compose_to').val('');
            }

            if (ccTagify && ccTagify.value.length > 0) {
                const ccEmails = ccTagify.value.map(tag => {
                    const match = tag.value.match(/<(.+?)>/);
                    return match ? match[1] : tag.email || tag.value;
                });
                $('#compose_cc').val(ccEmails.join(','));
            } else {
                $('#compose_cc').val('');
            }

            if (bccTagify && bccTagify.value.length > 0) {
                const bccEmails = bccTagify.value.map(tag => {
                    const match = tag.value.match(/<(.+?)>/);
                    return match ? match[1] : tag.email || tag.value;
                });
                $('#compose_bcc').val(bccEmails.join(','));
            } else {
                $('#compose_bcc').val('');
            }

            // Submit the form normally (not via AJAX to handle file uploads)
            this.submit();
        });

        // Initialize Tagify with AJAX search
        function initTagify(selector) {
            const input = document.querySelector(selector);

            if (!input) return null;

            const tagify = new Tagify(input, {
                whitelist: [],
                enforceWhitelist: false,
                maxTags: 10,
                dropdown: {
                    maxItems: 20,
                    classname: "tags-look",
                    enabled: 0,
                    closeOnSelect: false
                }
            });

            let controller;

            tagify.on('input', onInput);

            function onInput(e) {
                const value = e.detail.value;
                tagify.whitelist = null; // reset the whitelist

                if (value.length < 2) return;

                if (controller) controller.abort();
                controller = new AbortController();

                tagify.loading(true).dropdown.hide();

                fetch('/Employer/SearchUsers?query=' + encodeURIComponent(value), {
                    signal: controller.signal
                })
                .then(RES => RES.json())
                .then(function(newWhitelist) {
                    // Transform the data to what Tagify expects
                    const transformedList = newWhitelist.map(item => ({
                        value: `${item.fullName} <${item.email}>`,
                        searchBy: `${item.fullName} ${item.email}`,
                        name: item.fullName,
                        email: item.email
                    }));

                    tagify.whitelist = transformedList;
                    tagify.loading(false).dropdown.show(value);
                })
                .catch(err => {
                    if (err.name !== 'AbortError') {
                        console.error('Search error:', err);
                    }
                    tagify.loading(false);
                });
            }

            return tagify;
        }

        var toTagify = initTagify('#compose_to');
        var ccTagify = initTagify('#compose_cc');
        var bccTagify = initTagify('#compose_bcc');

        // CC/BCC toggles
        $('[data-kt-inbox-form="cc_button"]').click(function(e) {
            e.preventDefault();
            $('[data-kt-inbox-form="cc"]').removeClass('d-none').addClass('d-flex');
        });

        $('[data-kt-inbox-form="bcc_button"]').click(function(e) {
            e.preventDefault();
            $('[data-kt-inbox-form="bcc"]').removeClass('d-none').addClass('d-flex');
        });

        $('[data-kt-inbox-form="cc_close"]').click(function(e) {
            e.preventDefault();
            $('[data-kt-inbox-form="cc"]').removeClass('d-flex').addClass('d-none');
        });

        $('[data-kt-inbox-form="bcc_close"]').click(function(e) {
            e.preventDefault();
            $('[data-kt-inbox-form="bcc"]').removeClass('d-flex').addClass('d-none');
        });

        // Save as draft
        $('#btnSaveDraft').click(function (e) {
            e.preventDefault();
            $('#Body').val(quill.root.innerHTML);
            $('#kt_inbox_compose_form').attr('action', '/Employer/SaveDraft').submit();
        });

                // Attachments Handling
        Dropzone.autoDiscover = false;

        const composeForm = document.querySelector('#kt_inbox_compose_form');
        const attachmentBtn = document.querySelector('#btnAttachment'); // your paperclip button
        const dropzoneContainer = document.querySelector('#kt_inbox_reply_attachments');

        // Create a hidden input for files
        const fileInput = document.createElement('input');
        fileInput.type = 'file';
        fileInput.multiple = true;
        fileInput.style.display = 'none';
        fileInput.name = 'files';
        composeForm.appendChild(fileInput);

        // Keep selected files
        let selectedFiles = [];

        // Click paperclip → open file selector
        attachmentBtn.addEventListener('click', function (e) {
            e.preventDefault();
            fileInput.click();
        });

        // When files selected
        fileInput.addEventListener('change', function (e) {
            const files = Array.from(e.target.files);
            selectedFiles = selectedFiles.concat(files);
            displayAttachments();
        });

        // Display attachments below dropzone
        function displayAttachments() {
            let container = dropzoneContainer.querySelector('.dropzone-items');

            if (!container) return;

            container.innerHTML = ''; // Clear previous items

            selectedFiles.forEach((file, index) => {
                const fileSize = (file.size / 1024).toFixed(1); // KB
                const item = document.createElement('div');
                item.classList.add('dropzone-item', 'border', 'rounded', 'mb-2', 'd-flex', 'align-items-center');
                item.innerHTML = `
                    <i class="ki-duotone ki-file fs-2 me-3">
                        <span class="path1"></span><span class="path2"></span>
                    </i>
                    <div class="flex-grow-1">
                        <div class="fw-bold">${file.name}</div>
                        <div class="text-muted fs-7">${fileSize} KB</div>
                    </div>
                    <button type="button" class="btn btn-sm btn-icon btn-light-danger remove-file" data-file-index="${index}">
                        <i class="ki-duotone ki-cross fs-2"><span class="path1"></span><span class="path2"></span></i>
                    </button>
                `;
                container.appendChild(item);
            });

            // Add remove handlers
            container.querySelectorAll('.remove-file').forEach(btn => {
                btn.addEventListener('click', function () {
                    const index = parseInt(this.dataset.fileIndex);
                    selectedFiles.splice(index, 1);
                    displayAttachments();
                });
            });
        }

        // When submitting form, attach selected files
        composeForm.addEventListener('submit', function (e) {
            const dataTransfer = new DataTransfer();

            selectedFiles.forEach(f => dataTransfer.items.add(f));

            fileInput.files = dataTransfer.files;
        });
    });
    </script>
}